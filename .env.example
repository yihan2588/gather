NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Canonical origin used for OAuth callbacks (no trailing slash).
APP_URL=http://localhost:3000
# Local fictional-data mode is enabled only by pnpm dev:demo, never on Vercel.
